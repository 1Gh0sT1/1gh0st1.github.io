import { defineConfig } from 'vite'

export default defineConfig({
  // Задаем базовый путь равным названию репозитория на GitHub:
  base: '/photo-revive/', // Важно: это должно точно совпадать с именем вашего репозитория!
})
